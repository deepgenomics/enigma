name: enigma
channels:
  - conda-forge
  - bioconda
dependencies:
  - einops
  - genomekit
  - ipdb
  - ipykernel
  - jaxtyping
  - pandas
  - pip
  - python=3.12
  - wandb
  - pip:
    - torch==2.8.0
    - flash-attn==2.8.3  # if error occurs, try commenting this out and install separately
    - lightning==2.5.5